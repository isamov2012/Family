import Main from "./Main";
import SideBar from "./SideBar";

const Wrapper = () => {
  return (
    <div className="wrapper">
      <Main />
      <SideBar />
    </div>
  );
};

export default Wrapper;
